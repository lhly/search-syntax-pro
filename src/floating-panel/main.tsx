import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import '../styles/globals.css'

ReactDOM.createRoot(document.getElementById('floating-panel-root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
